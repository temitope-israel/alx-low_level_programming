#include "main.h"

/**
 * _abs - a function that
 * that computes the absolute value.
 * of an integer.
 * @v: Character to be checked
 *
 * Return: 1 upon success.
 */

int _abs(int v)
{
	int v;

	if (v < 0)
	{
		v = -v
		return (1);
	}
	else
	{
		return (0);
	}
}
