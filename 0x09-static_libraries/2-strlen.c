#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns
 * the length of a string..
 *
 * @s: parameter for update
 *
 * Return: Always 1 (Success)
 */


int _strlen(char *s)
{
	return (strlen(s));
}
